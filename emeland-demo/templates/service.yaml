apiVersion: v1
kind: Service
metadata:
  name: {{ include "emeland-demo.fullname" . }}
  labels:
    {{- include "emeland-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emeland-demo.selectorLabels" . | nindent 4 }}
